<Window x:Class="SFM.SimulatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SFM"
        mc:Ignorable="d"
        Title="Simulator" Height="600" Width="800">



    <Window.Resources>
        <local:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <local:NullToVisibilityConverter x:Key="NullToHiddenConverter"/>
        <local:EqualityConverter x:Key="EqualityConverter"/>

        <!-- ЦВЕТОВАЯ ПАЛИТРА QUESTSMITH -->
        <SolidColorBrush x:Key="BgDark" Color="#2B2B2B"/>
        <!-- Фон окна -->
        <SolidColorBrush x:Key="PanelDark" Color="#1A1A1E"/>
        <!-- Фон панелей -->
        <SolidColorBrush x:Key="AccentPurple" Color="#BB86FC"/>
        <!-- Фиолетовый акцент -->
        <SolidColorBrush x:Key="AccentTeal" Color="#03DAC6"/>
        <!-- Бирюзовый акцент (для +) -->
        <SolidColorBrush x:Key="TextMain" Color="#E0E0E0"/>
        <!-- Основной текст -->
        <SolidColorBrush x:Key="BorderDark" Color="#333338"/>
        <!-- Границы -->

        <!-- ОБНОВЛЕННЫЙ СТИЛЬ ВЕРХНИХ КНОПОК -->
        <Style x:Key="TopButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="10,0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder" 
                        BorderBrush="{StaticResource BorderDark}" 
                        BorderThickness="1" 
                        CornerRadius="12" 
                        Background="{StaticResource PanelDark}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Эффект при наведении -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#252529"/>
                                <Setter TargetName="BtnBorder" Property="BorderBrush" Value="{StaticResource AccentPurple}"/>
                            </Trigger>
                            <!-- Эффект при нажатии -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="{StaticResource AccentPurple}"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ОБНОВЛЕННЫЙ СТИЛЬ КНОПОК "+" -->
        <Style x:Key="PlusButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource AccentPurple}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder" CornerRadius="16" Background="{TemplateBinding Background}">
                            <TextBlock Text="+" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-2,0,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BtnBorder" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>


    <Border Background="{StaticResource BgDark}">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <!-- Шапка -->
                <RowDefinition Height="auto"/>
                <!-- Текст NPC -->
                <RowDefinition Height="*"/>
                <!-- Варианты игрока -->
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <!-- Шапка -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,30">
                <Border Width="60" Height="60" CornerRadius="30" Background="{StaticResource PanelDark}">
                    <TextBlock Text="🍺" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel Margin="20,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding NpcName}" Foreground="#BB86FC" FontSize="20" FontWeight="Bold"/>
                    <TextBlock Text="{Binding NpcStyle}" Foreground="#8E8E93" FontSize="12" FontStyle="Italic"/>
                </StackPanel>
                <Button Content="Сбросить" HorizontalAlignment="Right" Command="{Binding ResetCommand}" Style="{StaticResource TopButtonStyle}"/>
            </StackPanel>
            
            <!-- Текст NPC -->

            <Border Grid.Row="1" Background="{StaticResource PanelDark}" CornerRadius="15" Padding="30" BorderThickness="1" BorderBrush="#333">
                <TextBlock Text="{Binding CurrentNodeText}" Foreground="White" FontSize="22" 
                       TextWrapping="Wrap" VerticalAlignment="Center" TextAlignment="Center" FontStyle="Italic"/>
            </Border>

            <!-- Варианты игрока -->
            <StackPanel Grid.Row="2" Margin="0,30,0,0">
                <TextBlock Text="ВАРИАНТЫ ОТВЕТОВ ИГРОКА:" Foreground="{StaticResource TextMain}" FontSize="22" TextWrapping="Wrap" VerticalAlignment="Center" TextAlignment="Center" FontStyle="Italic"/>
                <ItemsControl ItemsSource="{Binding AvailableChoices}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.SelectChoiceCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}"
                                Margin="0,0,0,10" Padding="20,15" Background="#1A1A1E" BorderBrush="#333" BorderThickness="1">
                                <Button.Template >
                                    <ControlTemplate TargetType ="Button">
                                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                            BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="8">
                                            <Grid>
                                                <TextBlock Text="{Binding ChoiceText}" Foreground="White" FontSize="16" HorizontalAlignment="Left"/>
                                                <TextBlock Text="→" Foreground="#BB86FC" HorizontalAlignment="Right" FontSize="18"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#252529"/>
                                                <Setter TargetName="border" Property="BorderBrush" Value="#BB86FC"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Grid>
    </Border>

</Window>
